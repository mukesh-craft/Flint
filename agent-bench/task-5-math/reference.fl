fn fib(n: i64) -> i64 {
    if n <= 1 { n } else { fib(n - 1) + fib(n - 2) }
}

fn main() -> i64 {
    print(fib(20))
    mut p: i64 = 1
    for i in range(10) {
        p *= 2
    }
    print(p)
    mut s: i64 = 0
    for i in range(1, 11) {
        s += i * i
    }
    print(s)
    0
}
